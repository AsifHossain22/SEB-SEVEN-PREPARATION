let userNames: string[];

userNames = ["asif", "akashi", "ratul", "johora"];

console.log(userNames[0]);

userNames.sort();
console.log(userNames);

let multipleTypes: (string | number)[];
multipleTypes = [1, "ASIF"];
